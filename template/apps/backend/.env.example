# Local overrides for backend settings.
# Root .env (at project root) is the primary config — override specific vars here.
# DATABASE_URL=postgresql+asyncpg://app:app@localhost:54325/app
# SECRET_KEY=your-secret-key-here
